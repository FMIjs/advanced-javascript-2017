import { DirectDirective } from './direct.directive';

describe('DirectDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectDirective();
    expect(directive).toBeTruthy();
  });
});
